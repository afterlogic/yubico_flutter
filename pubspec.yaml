name: yubico_flutter
description: A new Flutter project.
version: 0.0.2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.afterlogic.yubico_flutter
        pluginClass: YubicoFlutterPlugin
      ios:
        pluginClass: YubicoFlutterPlugin
